# v5.0.0
**Основные изменения:**
- Добавлено явное определение Контроллера/Модели(API) вместо угадывания.<br>
  Ссылки для ajax запроса выглядят как /{module}/api/{action}, /auth/api/login.
- Добавлена NProgeress библиотека для отображения загрузки страницы.

**Другие изменения:**
- Исправлена типография в информации саппорт ветки.<br>
  *Спасибо BRONEPOEZD (discord: bronepoezd).*
- Добавлен модуль отправки информации в саппорт ветку.

---

# V4.1.0

**Общее:**
- (Server) plugin-manager
  - В контекст передан CLI.
- (Server) plugin-loader
  - Передан контекст CLI для логирования.
- (Server) config-manager
  - [TODO] Добавлен приватный централизованный метод `log` для логирования сообщений/ошибок.
- (Sever) command-registy
  - Обновлен метод `parseArgs()` - корректно принимает аргументы, добавлена фозможность использовать значения аргументов как `--key=value` так и `--key value`.

**Другое:**
- Исправлен висячий процесс в webpack/tailwindcss плагине.<br>
  Добавлены параметры запуска для контроля процесса.<br>
  В метод плагина `onDisable()` добавлена проверка Unix/Windows для лучшего контроля уничтожения процесса, это будет гарантировать 100% уничтожение процесса как на windows так и на unix системах.
- Другие незначительные исправления.